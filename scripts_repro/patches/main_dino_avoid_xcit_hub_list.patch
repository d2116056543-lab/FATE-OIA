diff --git a/main_dino.py b/main_dino.py
--- a/main_dino.py
+++ b/main_dino.py
@@ -32,6 +32,13 @@ torchvision_archs = sorted(name for name in torchvision_models.__dict__
     if name.islower() and not name.startswith("__")
     and callable(torchvision_models.__dict__[name]))
 
+try:
+    xcit_archs = torch.hub.list("facebookresearch/xcit:main")
+except Exception as exc:
+    print(f"Warning: unable to query XCiT torch.hub arch list ({exc}); "
+          "continuing with local ViT/torchvision choices.")
+    xcit_archs = []
+
 def get_args_parser():
     parser = argparse.ArgumentParser('DINO', add_help=False)
 
@@ -40,7 +47,7 @@ def get_args_parser():
     # Model parameters
     parser.add_argument('--arch', default='vit_small', type=str,
         choices=['vit_tiny', 'vit_small', 'vit_base', 'xcit', 'deit_tiny', 'deit_small'] \
-                + torchvision_archs + torch.hub.list("facebookresearch/xcit:main"),
+                + torchvision_archs + xcit_archs,
         help="""Name of architecture to train. For quick experiments with ViTs,
         we recommend using vit_tiny or vit_small.""")
     parser.add_argument('--patch_size', default=16, type=int, help="""Size in pixels
