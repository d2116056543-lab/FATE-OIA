# FATE-OIA GitHub Sync

This worktree intentionally ignores datasets, checkpoints and logs.

Placeholder:

```bash
git remote add fate git@github.com:<USER_OR_ORG>/FATE-OIA.git
git add .
git commit -m "Initialize FATE-OIA from SNNA with dataset-safe structure"
git push -u fate fate-oia
```
