actions:
  0: {name: forward, aliases: [go, move_forward], group: motion}
  1: {name: stop, aliases: [brake, halt], group: motion}
  2: {name: left, aliases: [turn_left, merge_left], group: lateral}
  3: {name: right, aliases: [turn_right, merge_right], group: lateral}
reasons:
  0: {name: "Traffic light is green", group: traffic_control, positive_predicates: [traffic_light_green, traffic_light_visible], contradictory_predicates: [traffic_light_red, stop_sign_present], compatible_actions: [forward], hard_negative_reasons: [3, 4], spatial_region: upper_traffic_region, tail: false}
  1: {name: "Follow traffic", group: road, positive_predicates: [front_vehicle_far, road_crowded, global_scene_context], contradictory_predicates: [road_clear], compatible_actions: [forward, stop], hard_negative_reasons: [2], spatial_region: front_center, tail: false}
  2: {name: "Road is clear", group: road, positive_predicates: [road_clear, drivable_center, ego_lane_centered], contradictory_predicates: [front_vehicle_close, pedestrian_front, obstacle_front, road_crowded], compatible_actions: [forward], hard_negative_reasons: [1, 5, 6, 8], spatial_region: front_center, tail: false}
  3: {name: "Traffic light", group: traffic_control, positive_predicates: [traffic_light_visible, traffic_light_red, traffic_light_green], contradictory_predicates: [], compatible_actions: [forward, stop], hard_negative_reasons: [2], spatial_region: upper_traffic_region, tail: false}
  4: {name: "Traffic sign", group: traffic_control, positive_predicates: [traffic_sign_visible, stop_sign_present], contradictory_predicates: [], compatible_actions: [forward, stop], hard_negative_reasons: [2], spatial_region: upper_traffic_region, tail: false}
  5: {name: "Obstacle: car", group: front_object, positive_predicates: [front_vehicle_close, front_vehicle_far, vehicle_left, vehicle_right], contradictory_predicates: [road_clear], compatible_actions: [stop, forward, left, right], hard_negative_reasons: [2], spatial_region: front_center, tail: true}
  6: {name: "Obstacle: person", group: front_object, positive_predicates: [pedestrian_front, crosswalk_region], contradictory_predicates: [road_clear], compatible_actions: [stop], hard_negative_reasons: [2], spatial_region: front_center, tail: true}
  7: {name: "Obstacle: rider", group: front_object, positive_predicates: [cyclist_front], contradictory_predicates: [road_clear], compatible_actions: [stop, forward], hard_negative_reasons: [2], spatial_region: front_center, tail: true}
  8: {name: "Obstacle: others", group: front_object, positive_predicates: [obstacle_front], contradictory_predicates: [road_clear], compatible_actions: [stop, left, right], hard_negative_reasons: [2], spatial_region: front_center, tail: true}
  9: {name: "No lane on the left", group: lane, positive_predicates: [left_lane_boundary, left_solid_boundary], contradictory_predicates: [lane_left_available, drivable_left, open_left_gap], compatible_actions: [forward, stop], hard_negative_reasons: [12], spatial_region: left_corridor, tail: true}
  10: {name: "Obstacles on the left lane", group: lane, positive_predicates: [parked_vehicle_left, vehicle_left], contradictory_predicates: [open_left_gap, drivable_left], compatible_actions: [forward, stop], hard_negative_reasons: [12], spatial_region: left_corridor, tail: true}
  11: {name: "Solid line on the left", group: lane, positive_predicates: [left_solid_boundary, left_lane_boundary], contradictory_predicates: [open_left_gap], compatible_actions: [forward, stop], hard_negative_reasons: [12], spatial_region: left_corridor, tail: true}
  12: {name: "On the left-turn lane", group: lane, positive_predicates: [left_turn_region, merging_left_context, lane_left_available], contradictory_predicates: [left_solid_boundary, vehicle_left], compatible_actions: [left], hard_negative_reasons: [9, 10, 11], spatial_region: left_corridor, tail: true}
  13: {name: "Traffic light allows left", group: traffic_control, positive_predicates: [traffic_light_green, traffic_light_visible, left_turn_region], contradictory_predicates: [traffic_light_red], compatible_actions: [left], hard_negative_reasons: [3], spatial_region: upper_traffic_region, tail: true}
  14: {name: "Front car turning left", group: front_object, positive_predicates: [front_vehicle_close, left_turn_region], contradictory_predicates: [road_clear], compatible_actions: [stop, forward], hard_negative_reasons: [2], spatial_region: front_center, tail: true}
  15: {name: "No lane on the right", group: lane, positive_predicates: [right_lane_boundary, right_solid_boundary], contradictory_predicates: [lane_right_available, drivable_right, open_right_gap], compatible_actions: [forward, stop], hard_negative_reasons: [18], spatial_region: right_corridor, tail: false}
  16: {name: "Obstacles on the right lane", group: lane, positive_predicates: [parked_vehicle_right, vehicle_right], contradictory_predicates: [open_right_gap, drivable_right], compatible_actions: [forward, stop], hard_negative_reasons: [18], spatial_region: right_corridor, tail: false}
  17: {name: "Solid line on the right", group: lane, positive_predicates: [right_solid_boundary, right_lane_boundary], contradictory_predicates: [open_right_gap], compatible_actions: [forward, stop], hard_negative_reasons: [18], spatial_region: right_corridor, tail: false}
  18: {name: "On the right-turn lane", group: lane, positive_predicates: [right_turn_region, merging_right_context, lane_right_available], contradictory_predicates: [right_solid_boundary, vehicle_right], compatible_actions: [right], hard_negative_reasons: [15, 16, 17], spatial_region: right_corridor, tail: false}
  19: {name: "Traffic light allows right", group: traffic_control, positive_predicates: [traffic_light_green, traffic_light_visible, right_turn_region], contradictory_predicates: [traffic_light_red], compatible_actions: [right], hard_negative_reasons: [3], spatial_region: upper_traffic_region, tail: false}
  20: {name: "Front car turning right", group: front_object, positive_predicates: [front_vehicle_close, right_turn_region], contradictory_predicates: [road_clear], compatible_actions: [stop, forward], hard_negative_reasons: [2], spatial_region: front_center, tail: false}
